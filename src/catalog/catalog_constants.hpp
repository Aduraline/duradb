#pragma once

#include <string_view>

namespace duradb {

inline constexpr std::string_view kPublicSchema = "public";
inline constexpr std::string_view kDefaultDatabase = "main";

} // namespace duradb
